import axios from "axios";

export const speechToTextSolo = async (file, kind) => {
  try {
    let a = await axios({ method: "Post", fd });
  } catch (err) {
    console.log(err);
    throw err;
  }
};
